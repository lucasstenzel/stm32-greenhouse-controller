/*
 * Minimal SystemInit stub. Called from Reset_Handler before main().
 * Leaves the chip on its reset-default clock (HSI, 16 MHz internal RC).
 * Replace with the full CMSIS version from
 *   C:/STM32/cmsis-device-f4/Source/Templates/system_stm32f4xx.c
 * when you need a configured clock tree (PLL, external crystal, etc.).
 */

void SystemInit(void) {
}
